#include<iostream>
#include<queue>
#include<list>
#include<deque>
using namespace std;


int main() {
	queue<int>q;
	q.push(60);
	q.push(50);
	q.push(40);
	q.emplace(30);
	cout << q.back();
	cout << q.front();
	while (!q.empty())
	{
		cout << q.front() << endl;
		q.pop();
	}
}

//add - delete : O(1)

